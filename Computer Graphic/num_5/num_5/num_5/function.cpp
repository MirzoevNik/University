#include "stdafx.h"
#include <cmath>

float f( float x )
{
	return sin ( x );
}

bool fexists( float x )
{
	/*if( cos ( x ) < 0.021 && cos ( x ) > -0.021 )
		return false;
	else
		return true;*/
	/*if ( x < 0.01 && x > -0.01 )
		return false;
	else
		*/return true;
}